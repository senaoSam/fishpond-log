# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.log

# Node (僅本地測試會用到,不該進 repo)
node_modules/
package-lock.json
yarn.lock
# 注意:pnpm-lock.yaml 要進 repo(鎖定版本),不要忽略

# Claude Code 個人本地設定(權限偏好,不進 repo);.mcp.json 則保留進 repo
.claude/settings.local.json

# Firestore 備份 / 遷移中間產物(含真實資料,不進公開 repo)
backups/
scripts/_migration-preview.json

# Playwright MCP 測試暫存
.playwright-mcp/
